name: CI

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.9'

#    - name: Install dependencies
#      run: if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#    - name: Clone repository
#      uses: actions/checkout@v3
#      with:
#        repository: dryabokon/tools
#        path: tools/

    - name: Run tests
      env:
        SECRET_LOGIN: ${{ secrets.SECRET_LOGIN }}

      run:
        run:
          echo "::add-mask::${{ secrets.SECRET_LOGIN }}"
          echo "your secret token is ${{ secrets.SECRET_LOGIN }}"

